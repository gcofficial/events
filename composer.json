{
    "name": "lf/events",
    "description": "Lolita Events",
    "type": "wordpress-plugin",
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.4"
    },
    "license": "MIT",
    "authors": [
        {
            "name": "Eugen Guriev",
            "email": "gurievcreative@gmail.com"
        }
    ],
    "scripts": {
        "install-dev": "COMPOSER_ALLOW_XDEBUG=0 composer install --no-interaction && git clone -b master https://github.com/WordPress-Coding-Standards/WordPress-Coding-Standards.git wpcs",
        "lint": "phpcs -p -s -v -n . --standard=./ruleset.xml --extensions=php"
    }
}
